import { Globe, type LucideIcon, Mail, MapPin, Phone } from "lucide-react";

export const iconMap: Record<string, LucideIcon> = {
	mail: Mail,
	phone: Phone,
	"map-pin": MapPin,
	globe: Globe,
};

export const A4_ASPECT = 210 / 297;
